{
  "name": "@trove/api",
  "version": "0.0.0",
  "private": true,
  "devDependencies": {
    "@trove/config": "workspace:*"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "lint": "eslint src --max-warnings=0",
    "typecheck": "tsc --noEmit --project tsconfig.json"
  }
}
